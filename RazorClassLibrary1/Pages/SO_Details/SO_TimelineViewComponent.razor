<RadzenTimeline>
    <Items>
        @foreach (var item in ServiceOrder.Registers.Reverse())
        {
            <RadzenTimelineItem PointStyle="@(item.CreatedAt.ToString() == maxRegister ? PointStyle.Primary : PointStyle.Base)">
                <LabelContent>
                    <RadzenText TextStyle="TextStyle.Subtitle2" class="rz-m-0">@item.CreatedAt</RadzenText>
                </LabelContent>
                <ChildContent>
                    @item.StateFrom - @item.StateTo | @item.Observations
                </ChildContent>
            </RadzenTimelineItem>
        }
    </Items>
</RadzenTimeline>